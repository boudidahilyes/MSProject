server:
  port: 8888
spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
#        - id: AUTHENTICATION
#          uri: lb:http://USER
#          predicates:
#            - Path=/api/v1/auth/**